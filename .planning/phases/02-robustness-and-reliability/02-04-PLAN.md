---
phase: 02-robustness-and-reliability
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - Reanalysis_Dashboard/app.py
autonomous: false
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, REL-01, REL-03]

must_haves:
  truths:
    - "app.py is deleted — Streamlit is no longer the entry point"
    - "User confirms the full wizard flow works in a real browser"
    - "Live log streaming updates during pipeline execution"
    - "Stop button cancels a running job and returns to Step 0"
    - "Error state shows plain-English message with collapsible traceback"
  artifacts:
    - path: "Reanalysis_Dashboard/server.py"
      provides: "FastAPI app serving the dashboard"
    - path: "Reanalysis_Dashboard/static/index.html"
      provides: "Alpine.js wizard UI"
  key_links:
    - from: "browser"
      to: "http://localhost:8000"
      via: "uvicorn serving server.py"
      pattern: "uvicorn"
---

<objective>
Delete the old Streamlit app.py and verify the complete end-to-end flow in a real browser.
This is the final plan that confirms Phase 2 is done.

Purpose: Remove the replaced Streamlit entry point and get user confirmation that the new
FastAPI + Alpine.js app works correctly.

Output: app.py deleted, user-verified working app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-robustness-and-reliability/02-CONTEXT.md
@.planning/phases/02-robustness-and-reliability/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Streamlit app.py</name>
  <files>Reanalysis_Dashboard/app.py</files>
  <read_first>
    - Reanalysis_Dashboard/app.py
    - Reanalysis_Dashboard/server.py
  </read_first>
  <action>
    Delete `Reanalysis_Dashboard/app.py`. This file is fully replaced by `server.py` + `static/index.html`.

    Verify no other file imports from app.py:
    ```bash
    grep -r "from app import\|import app" Reanalysis_Dashboard/ --include="*.py" | grep -v __pycache__
    ```
    If nothing references app.py, delete it with `git rm Reanalysis_Dashboard/app.py`.

    Run the test suite to confirm nothing breaks:
    ```bash
    cd Reanalysis_Dashboard && python -m pytest tests/ -x -q
    ```
  </action>
  <verify>
    <automated>python -c "import os; assert not os.path.exists('Reanalysis_Dashboard/app.py'), 'app.py still exists'; print('app.py deleted')" && cd Reanalysis_Dashboard && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ls Reanalysis_Dashboard/app.py` returns "No such file" (deleted)
    - `cd Reanalysis_Dashboard && python -m pytest tests/ -x -q` still passes
    - `grep -r "from app import" Reanalysis_Dashboard/ --include="*.py"` returns nothing
  </acceptance_criteria>
  <done>app.py deleted, no remaining references to it, test suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete wizard flow in browser</name>
  <files>Reanalysis_Dashboard/server.py, Reanalysis_Dashboard/static/index.html</files>
  <action>
    Start the server with `cd Reanalysis_Dashboard && python server.py` and present the
    verification checklist to the user. This is a human-verify checkpoint — the user tests
    the full wizard flow in their browser at http://localhost:8000.

    Verification checklist to present:
    1. Open http://localhost:8000 — wizard loads with Step 0 (Upload) visible
    2. Upload a model CSV — column dropdowns populate, preview table appears
    3. Upload an observation CSV — same behavior
    4. Click "Next: Hyperparameters" — Step 1 shows all controls with defaults
    5. Click "Run Reanalysis" — Step 2 shows live log lines streaming (not frozen)
    6. (Optional) Click Stop — returns to Step 0 with files preserved
    7. (If run completes) Step 3 shows summary metrics + "Run Another" button
    8. (Optional) Trigger an error — plain-English message + collapsible traceback
  </action>
  <verify>
    <automated>cd Reanalysis_Dashboard && python -c "from server import app; print('server importable')"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" after verifying the wizard flow
    - Server starts without error on `python server.py`
    - Browser loads the wizard UI at http://localhost:8000
  </acceptance_criteria>
  <done>User has verified the complete wizard flow works in a real browser — Phase 2 requirements confirmed</done>
</task>

</tasks>

<verification>
1. `Reanalysis_Dashboard/app.py` does not exist
2. `cd Reanalysis_Dashboard && python server.py` starts successfully on port 8000
3. Browser shows the wizard at http://localhost:8000
4. User confirms live streaming, cancel, and error handling work
</verification>

<success_criteria>
Phase 2 is complete when:
- Streamlit app.py is deleted
- FastAPI app runs with `python server.py`
- User has verified the full wizard flow in a real browser
- All 6 requirements (EXEC-01 through EXEC-04, REL-01, REL-03) are met
</success_criteria>

<output>
After completion, create `.planning/phases/02-robustness-and-reliability/02-04-SUMMARY.md`
</output>
